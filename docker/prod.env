# Production Environment Variables
# Usage: docker compose --env-file deployment/prod.env up -d

# --- Database ---
# Path inside the container (mapped to volume)
DATABASE_URL="file:/app/data/db.sqlite"

# --- Node Environment ---
NODE_ENV=production

# --- Auth.js (NextAuth.js v5) ---
# CRITICAL: Set this to your public URL (e.g. https://quiz.example.com)
AUTH_URL=http://localhost:3000
# Generate a secret with `openssl rand -base64 32`
AUTH_SECRET=changeme

# --- OIDC Configuration (Authentik, Keycloak, etc.) ---
# Uncomment and configure these to enable SSO
# AUTHENTIK_ISSUER=https://auth.example.com/application/o/bubble-quiz/
# AUTHENTIK_CLIENT_ID=your-client-id
# AUTHENTIK_CLIENT_SECRET=your-client-secret

# --- Backup Configuration ---
BACKUP_SCHEDULE="0 0 * * *"
BACKUP_DIR=/app/backups
BACKUP_RETENTION=5

# --- Optional Providers ---
# AUTH_GOOGLE_ID=
# AUTH_GOOGLE_SECRET=
# AUTH_DISCORD_ID=
# AUTH_DISCORD_SECRET=
